"""
pytest configuration file.

The presence of this file at the project root tells pytest to add this directory
to sys.path, so test modules can import the project's modules (categorizer,
budget_category, etc.) directly.
"""
